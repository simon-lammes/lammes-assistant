### This file was generated by Nexus Schema
### Do not make changes to this file directly


scalar DateTime

type Mutation {
  createNote(description: String, text: String!): Note
  deleteNote(noteId: Int!): Note
  editNote(deadlineTimestamp: String, description: String!, id: Int!, startTimestamp: String, text: String!): Note
  login(password: String!, username: String!): String
  register(firstName: String!, lastName: String!, password: String!, username: String!): Registration
  reopenNote(noteId: Int!): Note
  resolveNote(noteId: Int!): Note
}

type Note {
  creatorId: Int!
  deadlineTimestamp: DateTime
  description: String
  id: Int!
  resolvedTimestamp: DateTime
  startTimestamp: DateTime
  text: String!
  updatedTimestamp: DateTime!
  user: User!
}

input NoteWhereUniqueInput {
  id: Int
}

type Query {
  myDeferredNotes: [Note]
  myPendingNotes: [Note]
  myResolvedNotes: [Note]
  note(id: Int!): Note
  users: [User]
}

type Registration {
  jwtToken: String
  user: User
}

type User {
  firstName: String!
  id: Int!
  lastName: String!
  notes(after: NoteWhereUniqueInput, before: NoteWhereUniqueInput, first: Int, last: Int): [Note!]!
  username: String!
}
